# Recursion Example in Chevron v2.0 — Topo-Categorical DSL
# Countdown from 10 using recursive morphism chain
# Note: recursive fold is expressed as a pipeline with predicate transforms
10 → {> 0} → {- 1}
